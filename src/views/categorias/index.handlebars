<h2>Lista de Categorias:</h2>
<hr>
<a href="/categoria/create"><button class="btn btn-success">Nova Categoria</button></a>

{{#each category}}
<div class="card mt-4">
    <div class="card-body">
        <h4>Nome da categoria: {{name}}</h4>

        <small>Slug: {{slug}}</small><br>
        <small>Data de criação: {{ date}}</small><br>

        <a href="/categoria/update/{{_id}}"><button class="btn btn-warning mt-4">Editar categoria</button></a>
        <a href="/categoria/delete/{{_id}}"><button class="btn btn-danger mt-4">Deletar categoria</button></a>
    </div>
</div>
{{else}}
<h4 class="mt-3" style="text-align: center;">Nenhuma categoria registada.</h4>
{{/each}}